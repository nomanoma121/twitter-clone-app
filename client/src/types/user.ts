export type TUser = {
  id: number;
  display_id: string;
  name: string;
  icon_url: string;
  header_url: string;
  profile: string;
  follower_counts: number;
  followee_counts: number;
  followed_by_user: boolean;
  created_at: string;
};
